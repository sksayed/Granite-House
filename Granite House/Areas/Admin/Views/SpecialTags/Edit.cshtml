@model Granite_House.Models.SpecialTags
@{
    ViewData["Title"] = " Special Tag Edit";
}

<h2 class="text-info"> Edit a product </h2>

<form method="post" asp-action="Edit">
    <div class="p-4 border rounded">
        <input type="hidden" asp-for="TagId" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group row">
            <div class="col-1">
                <label asp-for="TagName"> TagName</label>
            </div>
            <div class="col-5">
                <input asp-for="TagName" class="form-control " />
            </div>
            <span asp-validation-for="TagName" class="text-danger"></span>

        </div>
        <br />
        <div class="form-group">
            <input type="submit" class="btn btn-primary" asp-route-id="@Model.TagId" value="Update" />
            <a asp-action="Index" class="btn btn-success"> Back To list </a>
        </div>
    </div>

</form>

@section scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}